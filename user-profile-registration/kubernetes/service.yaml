apiVersion: v1
kind: Service
metadata:
  name: user-profile-registration
  labels:
    app: user-profile-registration
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: user-profile-registration